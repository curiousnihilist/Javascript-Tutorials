// Arrays



// Array Declaration and Initialization
const friends = ['Akash','Ankit','Partho','Nilesh'];
const years = new Array(1991,1992,1993,1994);

//console.log(friends);
//console.log(years);


// ArrayMethods
/*
1. array.length = returns length of array
2. array.push(element) = adds element at the end
3. array.pop() = remove element from the end
4. array.unshift(element) = adds element at the beginning
5. array.shift() = remove element from the beginning
6. array.indexOf(element) = returns index of element and -1 if it doesn't exist
7. array.lastIndexOf(element)
8. array.includes(element)
9. array.some(element)
10. array.at(element)
etc. etc.
Link: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array
*/

var fruits = ['apple','mango','orange','litchi','watermelon']


// Array Methods

console.log(fruits.toString());
console.log(`array.length -> ${fruits.length}`);
console.log(`array.at() -> ${fruits.a}`);